# ConcurShield

AI 驱动的企业报销审核平台。设计目标：让报销系统里的 AI **做该做的事，不做不该做的事**。

> 核心设计原则：区分 Workflow 和 Agent、按观众分层、工具白名单防 prompt injection。
> 参照 [Anthropic *Building Effective Agents*](https://www.anthropic.com/research/building-effective-agents)（2024）。

---

## 架构总览

```
                      ┌──────────────────────────────────────────┐
                      │           3 种 Agent 形态                 │
                      │                                          │
  员工 submit 页 ─────▶  Agent 1: 全交互式 Chat (SSE)            │
  上传发票，AI 填字段     agent_role = employee_submit             │
                      │  tools: extract / suggest / check_dup /  │
                      │         get_history / update_draft       │
                      └──────────────────┬───────────────────────┘
                                         │ 用户点"提交"
                                         ▼
                               POST /api/submissions
                                         │ BackgroundTask
                                         ▼
                         ┌───────────────────────────────┐
                         │     concurshield-agent        │
                         │     5-Skill 审核 Workflow      │
                         │  skill_01 发票验证             │
                         │  skill_02 额度审批             │
                         │  skill_03 合规 + AmbiguityDet  │
                         │  skill_04 凭证生成             │
                         │  skill_05 支付处理             │
                         └───────────────┬───────────────┘
                                         │ tier / risk_score / timeline[0:3]
                                         ▼ status = reviewed
  ┌──────────────────────────────────────────────────────────┐
  │                    审批流                                 │
  │  经理审批  POST /api/submissions/{id}/approve             │
  │         → append timeline[3]: 凭证已生成                 │
  │                    │                                    │
  │  财务审批  POST /api/finance/submissions/{id}/approve     │
  │         → append timeline[4]: 付款已执行                 │
  └──────────────────────────────────────────────────────────┘

  员工 my-reports 页 ──▶  Agent 2: 只读 QA Drawer (SSE, stateless)
                         agent_role = employee_qa
                         tools: get_recent / get_detail / get_summary
                         【无任何写操作，prompt injection 无法写 DB】

  经理/财务 审批页 ─────▶  Agent 3: 嵌入式 AI 解释卡 (单次 JSON)
                         agent_role = manager_explain
                         tools: get_submission_for_review / get_employee_history
                         【10 秒/单决策，不是对话，不降低审批吞吐】
```

---

## 快速启动（不需要任何 API Key）

```bash
# 1. 安装依赖
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# 2. 启动服务（默认 MockLLM 模式，全本地，无需 API Key）
uvicorn backend.main:app --reload --port 8000

# 3. 访问（浏览器直接打开）
# 员工入口:   http://localhost:8000/employee/submit.html
# 经理入口:   http://localhost:8000/manager/queue.html
# 财务入口:   http://localhost:8000/finance/review.html
# 管理员:     http://localhost:8000/admin/dashboard.html
```

> **Mock 模式**：`AUTH_MODE=mock`（默认），用 `X-User-Id`/`X-User-Role` 请求头模拟身份。
> 浏览器端通过导航栏的角色切换下拉框（👤/👔/💼）或 URL 参数 `?as=manager` 切换角色。

---

## 5 分钟 Demo 演练

```bash
# 0. 可选：先植入演示数据（5张状态各异的单据）
python scripts/seed_demo_data.py
```

**完整闭环路径（手动）：**

| 步骤 | URL 参数 | 操作 |
|------|---------|------|
| 1. 员工提交 | `?as=employee` | 打开 submit.html，上传任意图片，点 AI 识别，确认后提交 |
| 2. AI 审核 | — | 自动（1–3 秒），my-reports 页轮询直到 status=reviewed |
| 3. 经理审批 | `?as=manager` | queue.html 点开报销单，看右侧 AI 解释卡，点通过 |
| 4. 财务审批 | `?as=finance_admin` | finance/review.html，同上，通过后生成凭证号 |
| 5. 导出 | `?as=finance_admin` | finance/export.html 批量导出 CSV |

**开发者模式（看 AI 内部 trace）：**

在任意页面 URL 加 `?dev=1` 或点击导航栏 🔧 按钮，AI 解释卡底部会显示：
- 实际调用了哪些 tool
- agent_role 是什么

---

## 核心设计决策

### 1. Workflow vs Agent：诚实申报

> "Agentic AI" 这个词被滥用。这里明确标出哪里是 agent，哪里是 workflow。

| 位置 | Mock 当前实现 | 实质 | 原因 |
|---|---|---|---|
| 员工 submit：happy path（上传发票→填字段）| OCR → dup_check → suggest → write，4 步线性 | **Workflow** | 步骤预定，LLM 不需要决策 |
| 员工 submit：用户改字段（"把金额改成 380"）| 架构就绪，等 Real LLM | **真 Agent** | LLM 需要解析意图、动态选 tool |
| 员工 my-reports QA drawer | 关键词匹配 → 单 tool → 格式化 | **Workflow** | 单 tool 调用，无决策环节 |
| 5-Skill 审核管道 | 5 步顺序执行，policy_engine 硬规则 | **Workflow（故意）** | 合规要求确定性，不允许 LLM 改流程 |
| 经理/财务 AI 解释卡 | 调 2 个只读 tool，规则组装解释 | **Agent（轻量）** | 需自行决定调哪些 tool 收集证据 |

**面试讲稿**：
> "Submit 页 happy path 严格来说是 workflow——5 步线性管道，LLM 没有决策环节。真正需要 agent 的是'用户改字段'场景：解析 '把金额改成 380'、动态选 tool、可能多步组合。我让两种形态共存在同一个 run_agent loop 里，MockLLM 只实现 workflow 部分保持 deterministic eval；Real LLM 接上就激活 agent 行为。"

---

### 2. 工具白名单（Prompt Injection 防线）

`TOOL_REGISTRY` 把每个 `agent_role` 映射到它允许调用的 tool 名字列表。白名单在两处强制执行：

1. **LLM 只看到白名单内的 tool 定义**（喂给 `tools=` 参数的列表被过滤）
2. **Dispatcher 二次校验**：即使 LLM 幻觉出白名单外的 tool 名，dispatch 前也会被拒绝

```python
TOOL_REGISTRY = {
    "employee_submit": ["extract_receipt_fields", "suggest_category",
                        "check_duplicate_invoice", "get_my_recent_submissions",
                        "update_draft_field"],          # 有写权限
    "employee_qa":     ["get_my_recent_submissions",
                        "get_report_detail", "get_spend_summary"],  # 全只读
    "manager_explain": ["get_submission_for_review",
                        "get_employee_submission_history"],          # 全只读
}
```

> Agent 永远没有 `submit_expense` / `approve` 工具。这不是 limitation，是设计决策。

---

### 3. 渐进式审计时间线（Phased Timeline）

**问题**：原实现在提交时就把 5-skill 全部结果写入 `audit_report.timeline`，导致"凭证生成 / 付款执行"在经理批准之前就出现在 AI 解释卡里。

**Option B 修复**：

```
提交后         timeline = [step0, step1, step2]          phase="submit"
经理批准后     timeline.append(step3: "凭证已生成")      phase="manager_approved"
财务批准后     timeline.append(step4: "付款已执行")      phase="finance_approved"
```

`audit_report.timeline` 永远只反映"已经发生的事"。AI 解释卡的 green_flags 不会展示未来的步骤。

---

### 4. 按观众分层（Audience Layering）

AI 解释卡的信息分两层：

| 信息层 | 谁看 | 展示条件 |
|---|---|---|
| 推荐决策、flags、advisory | 所有用户（经理/财务）| 始终显示 |
| tool 调用明细、agent_role | 开发者 / 面试官 | `auth.isDev()` = true |

**激活 dev 模式**：URL 加 `?dev=1`，或点击导航栏 🔧 按钮。
**关闭 dev 模式**：再次点击 🔧，或 URL 加 `?dev=0`。

---

## Eval 测试体系

```bash
# 运行全部 62 个测试（~1 秒，无需外部服务）
pytest backend/tests/ -v

# 只跑 eval harness（12 个业务场景，带通过率汇总）
pytest backend/tests/test_agent_eval.py -v -s
```

**Eval Harness 结构**（`backend/tests/eval_cases.yaml` + `test_agent_eval.py`）：

| 类别 | 用例数 | 验证内容 |
|---|---|---|
| QA 意图路由 | 4 | 月度/季度消费、最近记录、无关问题 → 正确 tool 或无 tool |
| 解释卡 tier 映射 | 3 | T1→approve、T3→review、T4→reject + advisory 内容 |
| 角色访问控制 | 2 | 员工 403、财务 200 |
| 白名单安全注入 | 3 | 注入 forbidden tool → 被 dispatcher 拦截，返回 error |

新增业务场景只需在 YAML 里加一行，不需要改 Python。

---

## 我们故意不做的 5 件事

| # | 没做的事 | 为什么 | 触发条件 |
|---|---|---|---|
| 1 | **5-Skill 管道改成 Agent** | 合规要求确定性可审计，把法律责任从规则转移到 LLM | 永远不做 |
| 2 | **Agent 拥有 submit / approve 工具** | 白名单是防注入最后一道闸，破开 = 拆防线 | 永远不做 |
| 3 | **审批页加 chat drawer** | 经理一天审 30 单，多 30 秒/单 = 一天多 15 分钟，经理直接关 AI | 永远不做 |
| 4 | **一上来就接 Agent SDK** | MVP 原生 tool calling 够用；SDK 价值在 subagents/memory，当前用不上 | 有 subagent 需求时迁移 |
| 5 | **AI 自动提交报销** | 法律责任在员工，Submit 必须由人 confirm。SAP Concur Joule 2026 同样决策 | 永远不做 |

---

## API 总览

**报销单**

| 方法 | 路径 | 角色 | 说明 |
|------|------|------|------|
| `POST` | `/api/submissions` | employee | 提交报销单，返回 202 + 后台 AI 审核 |
| `GET` | `/api/submissions/{id}` | all\* | 查询单条（员工只能查自己的） |
| `GET` | `/api/submissions` | all | 列表（员工只见自己的） |
| `POST` | `/api/submissions/{id}/approve` | manager | 经理批准 → manager_approved |
| `POST` | `/api/submissions/{id}/reject` | manager | 经理拒绝 |
| `POST` | `/api/submissions/bulk-approve` | manager | 批量批准 |
| `POST` | `/api/finance/submissions/{id}/approve` | finance_admin | 财务批准 → finance_approved + 凭证号 |
| `POST` | `/api/finance/submissions/{id}/reject` | finance_admin | 财务拒绝 |
| `POST` | `/api/finance/submissions/bulk-approve` | finance_admin | 批量财务批准 |
| `GET` | `/api/finance/export/preview` | finance_admin | 待导出列表 |
| `POST` | `/api/finance/export` | finance_admin | 批量导出 CSV |

**Agent / Chat**

| 方法 | 路径 | 角色 | 说明 |
|------|------|------|------|
| `POST` | `/api/chat/drafts` | employee | 新建 draft |
| `POST` | `/api/chat/drafts/{id}/receipt` | employee | 上传发票到 draft |
| `POST` | `/api/chat/drafts/{id}/message` | employee | Agent 1：submit chat（SSE） |
| `POST` | `/api/chat/drafts/{id}/submit` | employee | Draft 转为正式 submission |
| `POST` | `/api/chat/qa/message` | employee | Agent 2：只读 QA（SSE，stateless） |
| `POST` | `/api/chat/explain/{id}` | manager / finance_admin | Agent 3：AI 解释卡（JSON） |

**管理**

| 方法 | 路径 | 角色 | 说明 |
|------|------|------|------|
| `GET/PUT` | `/api/admin/policy` | finance_admin | 报销政策 |
| `GET` | `/api/admin/audit-log` | finance_admin | 审计日志（分页） |
| `GET` | `/api/admin/stats` | finance_admin | 汇总统计 |
| `GET` | `/api/users/me` | all | 当前用户信息 |
| `GET` | `/health` | — | 健康检查 |

---

## 风险等级

| 等级 | AI 推荐 | 风险分 | 含义 |
|------|---------|--------|------|
| T1 | approve | ≤25 | 发票合规、金额正常、描述具体 |
| T2 | approve | 25–50 | 低风险，有轻微注意项 |
| T3 | review  | 50–75 | 需人工核对，金额偏高或描述模糊 |
| T4 | reject  | >75 | 高风险，金额异常 / 凭证缺失 |

---

## 目录结构

```
expense-ai-agent/
├── backend/
│   ├── api/
│   │   ├── middleware/auth.py          # Mock / Clerk 双模式认证
│   │   └── routes/
│   │       ├── submissions.py          # 提交报销 + 异步 AI 管道
│   │       ├── approvals.py            # 经理审批 + 渐进 timeline
│   │       ├── finance.py              # 财务审批 + 凭证号 + CSV 导出
│   │       ├── chat.py                 # 3 种 Agent（submit/QA/explain）
│   │       ├── ocr.py                  # OCR（mock → Anthropic Vision）
│   │       ├── admin.py                # policy / audit-log / stats
│   │       └── users.py                # /me
│   ├── db/store.py                     # SQLAlchemy async ORM + CRUD
│   ├── storage.py                      # LocalStorage / R2Storage 抽象
│   ├── config.py                       # 环境变量
│   ├── main.py                         # FastAPI 入口 + 静态文件
│   └── tests/                          # 62 个测试（按模块隔离 DB）
│       ├── eval_cases.yaml             # Eval harness 用例（可直接扩展）
│       ├── test_agent_eval.py          # 业务场景 eval，12 cases
│       ├── test_phased_timeline.py     # 渐进 timeline 回归，2 cases
│       ├── test_chat_explain.py        # AI 解释卡，6 cases
│       ├── test_chat_qa.py             # QA drawer，3 cases
│       └── ...                         # auth / submissions / approvals 等
├── frontend/
│   ├── shared/
│   │   ├── auth.js                     # Mock / Clerk 双模式 + isDev() + 角色切换
│   │   ├── api.js                      # fetch 封装（getHeaders / qaStream / getExplanation）
│   │   ├── nav.js                      # 动态导航 + 🔧 dev 模式切换
│   │   ├── ai-card.js                  # AI 解释卡组件（含观众分层渲染）
│   │   ├── ai-card.css                 # 解释卡样式（tier badge / flag colors）
│   │   └── styles.css                  # 全局样式
│   ├── employee/
│   │   ├── submit.html                 # Agent 1：全交互式 chat + draft 提交
│   │   └── my-reports.html             # Agent 2：只读 QA drawer（右下角）
│   ├── manager/
│   │   └── queue.html                  # Agent 3：审批队列 + AI 解释卡
│   ├── finance/
│   │   ├── review.html                 # Agent 3：财务复核 + AI 解释卡
│   │   └── export.html                 # 批量导出入账
│   └── admin/
│       ├── dashboard.html / policy.html / employees.html
│       ├── audit-log.html / users.html
│       └── ...
├── scripts/
│   └── seed_demo_data.py               # 一键植入演示数据（5 张状态各异的单据）
├── concurshield-agent/                 # 5-Skill 审核引擎（独立子模块）
├── Dockerfile / docker-compose.yml
└── .env.example
```

---

## 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `AUTH_MODE` | `mock` | `mock`（开发）\| `clerk`（生产）|
| `DATABASE_URL` | SQLite 本地文件 | 生产用 `postgresql+asyncpg://...` |
| `STORAGE_BACKEND` | `local` | `local` \| `r2`（Cloudflare R2）|
| `ANTHROPIC_API_KEY` | — | **可选**：Claude Vision OCR 兜底 |
| `AGENT_USE_REAL_LLM` | — | 设为 `1` + 提供 API Key → 切换 RealLLM（当前为 stub）|
| `CLERK_SECRET_KEY` | — | `AUTH_MODE=clerk` 时必填 |

### MockLLM vs RealLLM（GPT-4o）

| 模式 | 条件 | 行为 |
|---|---|---|
| **MockLLM**（默认）| 无需 API Key | 规则脚本：happy path 线性跑完，keyword routing，deterministic eval |
| **RealLLM（GPT-4o）** | `OPENAI_API_KEY` + `AGENT_USE_REAL_LLM=1` | GPT-4o 真实推理 + Vision OCR，解锁"用户改字段"Agent 行为 |

**切换到 GPT-4o：**

```bash
# .env
OPENAI_API_KEY=sk-...
AGENT_USE_REAL_LLM=1
# 可选：OPENAI_MODEL=gpt-4o-mini（速度更快、费用更低）
```

**RealLLM 激活后的变化：**
- `tool_extract_receipt_fields` → 调用 GPT-4o Vision 识别真实发票图片
- Agent 的 tool 选择决策 → GPT-4o 真实推理（不再是关键词规则）
- 解锁"把金额改成 380"/"类别改成娱乐" 等自然语言字段修改

> 架构无需改动：`run_agent` loop、工具白名单、SSE streaming 全部不动，只换 LLM backend。

---

## Backlog

| # | 需求 | 优先级 | 工作量 | 触发条件 |
|---|---|---|---|---|
| 1 | **接入 Claude API（RealLLM）**：实现 `RealLLM.next_turn()`，解锁用户改字段 Agent 行为 | P1 | 2h | 有 API Key 时 |
| 2 | **真实 OCR**：`extract_receipt_fields` 接 Claude Vision，替换 mock 数据 | P1 | 1h | 有 API Key 时 |
| 3 | **GL 科目映射可扩展**：admin 页 CRUD 自定义费用类别 + 会计科目 | P2 | 1.5h | 进 prod / 客户问"能加类别吗" |
| 4 | **角色感知错误页**：无权限时整页跳转 + 解释 + 切角色入口 | P3 | 1h | 进 prod 前 |
| 5 | **Submit 表单丰富**：项目 picker、多附件、备注 | P3 | 2h | polish 阶段 |
| 6 | **语音输入**：Web Speech API，submit 页麦克风按钮 | P4 | 1h | 客户在移动/车间场景 |
