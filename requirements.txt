# ── Web framework ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── Database ────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0          # SQLite async（本地开发）
asyncpg>=0.29.0            # PostgreSQL（生产）

# ── Auth ────────────────────────────────────────────────────────
clerk-backend-api>=1.0.0
python-jose[cryptography]>=3.3.0   # JWT 验证

# ── File storage ────────────────────────────────────────────────
boto3>=1.34.0              # Cloudflare R2 via S3 兼容 API

# ── Concurshield-agent 依赖 ─────────────────────────────────────
pyyaml>=6.0.1

# ── LLM / OCR ────────────────────────────────────────────────────
openai>=1.30.0             # GPT-4o RealLLM + Vision OCR
anthropic>=0.30.0          # 预留 Anthropic 接口
Pillow>=10.4.0
imagehash>=4.3.1

# ── 通用工具 ────────────────────────────────────────────────────
pydantic>=2.9.0
python-dotenv>=1.0.0
httpx>=0.27.0              # 外部 API 调用（Clerk JWKS）

# ── 测试 ────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0              # FastAPI TestClient

# ── PDF 处理 ────────────────────────────────────────────────────
pypdf>=4.0.0              # PDF page splitting for multi-receipt uploads
